# 학령인구 대시보드 개선 진행상황

갱신: 2026-08-26  
대상: `prototype/index.html` 및 Cloudflare Pages 배포 구성

## 이번 결정 반영

- [x] 유치원 수를 `614`로 고정하지 않고 내장된 유치원 자료의 실제 개수를 세어 표시
  - 경북 전체에서는 614곳, 시군 상세에서는 해당 시군 값으로 전환(예: 영주시 23곳)
  - 초·중·고·유치원·특수학교 다섯 학교급을 모두 같은 방식으로 갱신
- [x] 공개 화면에 교원 수 KPI 추가
  - 초·중·고·특수학교는 학교알리미 공시값 사용
  - 유치원 교원 수는 현재 자료에 없으므로 `자료 미확보`로 표시
  - 전체 조회는 `유치원 제외`라고 명시
- [x] 다문화 및 특수교육 메뉴 유지
- [x] 출처 없는 소멸위험지수의 버튼·지도 계산·범례·시군 배지 삭제
- [x] 학교 순번으로 동·읍·면을 임의 배정하던 가짜 소재지 필터 삭제 및 비활성화
- [x] 임의 성별 분할 차트를 학교알리미 실제 학년별 학생수 차트로 교체
- [x] Gemini 3.7 Flash 분석 화면과 같은 출처 서버 중계 추가
- [x] 모바일 720px 이하 레이아웃 보완
- [x] 배포 저장소에 Cloudflare Pages Function이 함께 복사되도록 자동 배포 수정
- [x] API 키 파일 패턴을 `.gitignore`에 추가하고 키가 정적 HTML로 나가지 않도록 유지
- [x] KOSIS 기관 ID·통계표 ID를 직접 확인해 설정 파일과 자동 수집 도구에 반영
  - 기관 ID: `101`
  - 출생아·합계출산율: `DT_1B81A23`
  - 1세별 주민등록인구: `DT_1B04006`
  - 경북 시군 장래인구추계: `INH_PB0001_2022_37`
  - 5세별 순이동: `DT_1B26002`
- [x] KOSIS 2016~2025 주민등록인구와 2026~2036 장래인구추계를 대시보드 학령인구 시계열에 반영
- [x] SGIS 온라인 지도와 기존 SVG 오프라인 대체 지도를 선택할 수 있게 구현
  - SGIS 서비스 ID는 Cloudflare Pages Function에서만 사용
  - SGIS 응답·CSS는 같은 출처로 중계하고 정적 HTML에는 키를 넣지 않음
  - 온라인 로드 실패 시 오프라인 경계 지도를 유지
  - 로컬 Pages 환경에서 실제 지도 타일, 학교 좌표 1,538곳, 군집 클릭·확대와 개별 학교급 마커까지 확인
  - 군집 마커는 외부 이미지에 의존하지 않는 36px 숫자 마커로 보강
- [x] 유치원알리미 `기본현황(신규)` API의 공식 좌표로 갱신
  - 공식 좌표 613곳, 누락 1곳만 기존 주소 변환 좌표 유지
- [x] 굽기 스크립트의 무조건 라이브 배포를 중지하고, 명시적 `DEPLOY_LIVE=1`일 때만 수동 배포하도록 변경

## 배포 전에 해야 할 일

1. 대화에 노출된 모든 API 키를 각 발급기관에서 재발급 또는 폐기 후 재발급한다.
2. [Cloudflare Pages 프로젝트](https://dash.cloudflare.com/)의 **Settings → Variables and Secrets**에서 `GEMINI_API_KEY`와 `SGIS_CONSUMER_KEY`를 우선 암호화 Secret으로 등록한다. 나머지 키도 같은 이름으로 등록해 다음 데이터 갱신에 대비한다.
3. Google Cloud의 해당 키를 **Generative Language API(Gemini API)로만 제한**하고 결제 계정을 연결하지 않는다. 무료 한도 초과 시 앱은 오프라인 요약으로 전환한다.
4. 무료 등급에서 전송 데이터가 Google 제품 개선에 사용될 수 있으므로 AI에는 집계값만 보내고 개인자료·학교 내부 비공개자료는 보내지 않는다.
5. 배포 후 `/dashboard/#sim`에서 `현재 결과 분석하기`를 눌러 응답과 한도 초과 메시지를 확인한다.

로컬 개발 키는 저장소 루트의 `.dev.vars`에 이미 정리했다. 이 파일은 커밋되지 않으며, 공유용 이름만 `.dev.vars.example`에 있다.

6. Cloudflare Pages를 GitHub `Kimmahone/team-leap`의 `main` 브랜치와 연결한다. 연결 전에는 GitHub Actions가 배포 저장소를 갱신해도 라이브 사이트가 자동으로 바뀌지 않는다.

## 추가 자료·결정 대기

### 온라인 지도

- [x] SGIS 서비스 ID와 보안 Key 한 쌍을 전달받았다. 화면에 노출된 값은 운영 적용 전에 재발급하고 Cloudflare Secret으로 등록해야 한다.
- [x] SGIS 온라인 지도 구현과 오프라인 대체 지도 전환을 완료했다.
- [SGIS 개발지원센터](https://sgis.kostat.go.kr/developer/html/openApi/api/intro.html)의 **인증키 발급 현황**에서 서비스 ID(`consumer_key`), 보안 Key(`consumer_secret`), 허용 도메인을 확인한다.
- [ ] 운영용 SGIS 키를 재발급한 뒤 허용 도메인에 실제 Pages 주소를 등록하고 라이브에서 타일·마커를 확인한다.

### 유치원

- [x] 유치원알리미 Open API 인증키와 `기본현황`, `직위·자격별 교직원현황` 제공 주소를 확인했다. 화면에 노출된 값은 운영 적용 전에 재발급한다.
- [x] `기본현황(신규)` 공식 좌표로 614곳을 다시 수집했다.
- [ ] `직위·자격별 교직원현황` API는 현재 제공기관의 데이터 점검으로 `DENIED`를 반환한다. 제공 재개 뒤 다시 굽기 전까지 교원 수를 추정하지 않고 `자료 미확보`로 둔다.
- [유치원알리미 Open API 이용안내](https://e-childschoolinfo.moe.go.kr/openApi/openApiIntro.do)에서 개발 또는 운영계정 인증키를 신청하고, [API 제공목록](https://e-childschoolinfo.moe.go.kr/openApi/openApiList.do)에서 교직원 현황 제공 항목·공시 차수를 확인한다.
- 오래된 공시 110곳의 운영 여부는 해당 교육지원청 또는 도교육청 내부 명부로 교차 확인한다.

### KOSIS·학교알리미

- [학교알리미 Open API](https://www.schoolinfo.go.kr/ng/go/pnnggo_a01_m0.do)의 2026 학교·학생·학급·교원 공시는 현재 내장되어 있다. 새 키는 다음 공시 갱신 때 사용한다.
- [x] [KOSIS 공유서비스](https://kosis.kr/openapi/index/index.jsp)의 기관 ID·통계표 ID·연령코드를 확정했고 `open api/kosis-tables.json`에 보관했다. 사용자가 KOSIS 화면에서 더 찾을 작업은 없다.
- [x] `open api/bake-kosis.mjs`로 공식 시계열을 내려받고 대시보드에 굽는 경로를 만들었다.
- 코호트 예측을 위해 최소 2016~2026년 출생아수와 학교알리미 학년별 학생수 과거 공시가 필요하다.

### 경북교육청 내부자료

- 상세한 우선순위·필드·예상 부서·공개 범위는 [경북교육청 내부자료 요청 목록](./경북교육청_내부자료_요청목록.md)에 정리했다.
- 현재 추가 자료 폴더는 정책 근거와 업무 절차를 보강했지만, 학교·연도별 원자료를 대신하지는 않는다.
- 공식 홈페이지 연계 방식은 추후 결정. 그 전까지 데이터 스키마와 API 경계를 분리해 유지한다.

## 아직 공개 판단에 쓰면 안 되는 부분

- 2027년 이후 학생 수는 코호트 예측이 아니라 임시 감소율을 적용한 모의값이다.
- 학령인구 시계열은 KOSIS 공식값으로 교체했지만, 2027년 이후 **학생 수** 시뮬레이션은 여전히 임시 감소율 모의값이다.
- 인구감소지역 표시는 기존 자료이며 출처·기준연도 재검증이 필요하다.
- 다문화 학생 수는 아직 자료가 없어 비워 두었다.

## 검사 결과

- 대시보드 자동 검사: 126개 통과
- Gemini 중계 검사: 9개 통과
- KOSIS 수집 검사: 5개 통과
- SGIS 중계 검사: 9개 통과
- 전체 사이트 굽기: 2,417개 검사 전부 통과, 인증키 유출 0건, 정적 외부 CDN 0건
- SGIS 실제 인증 응답: HTTP 200, 약 320KB 지도 스크립트 수신, 서비스 ID가 응답에 포함되지 않음을 확인
- 로컬 Pages 화면 확인: 온라인 SGIS 지도 1,538곳·36개 초기 군집, 유치원 614곳, KOSIS 2026 합계 252천 명 정상 표시
- 2026-08-26 라이브 `team-leap.pages.dev` 자동 배포 완료. 영주시 상세의 학교급별 숫자와 유치원 23곳, 온라인 SGIS 지도의 영주시 유치원 마커 23곳을 실제 브라우저에서 확인했다.
- 라이브 `/api/sgis-map`은 HTTP 200, `/api/ai-analysis` 함수는 정상 배포됐지만 Gemini 상류 호출은 45초 뒤 504 시간 초과가 난다. 공식 OpenAI 호환 경로와 `reasoning_effort: low`까지 적용했으므로, Google AI Studio에서 해당 키의 **차단 여부·프로젝트 상태·무료 사용량**을 확인한 뒤 다시 검증한다.
- 모바일 브라우저의 최종 육안 확인은 배포 전 390px·768px 두 너비에서 한 차례 더 수행한다.
