# 학령인구 대시보드 개선 진행상황

갱신: 2026-08-27
대상: `prototype/index.html` 및 Cloudflare Pages 배포 구성

## 2026-08-27 기능개선 재점검·출력·다중필터

- [x] 행정안전부 `인구감소지역 지정` 문서 기준 경북 15개 시군으로 교체
  - 고령, 문경, 봉화, 상주, 성주, 안동, 영덕, 영양, 영주, 영천, 울릉, 울진, 의성, 청도, 청송
  - 기존에 잘못 포함됐던 예천군 제외
- [x] 실제 위치 지도의 `학생 수` 모드 수정
  - 군집 숫자를 학교 개수가 아닌 학생 합계로 계산
  - 학생 합계에 따라 군집과 개별 마커 크기가 반응하도록 보완
- [x] 시뮬레이터 다중 조건 필터 구현
  - 인구감소지역, 시/군, 직접 선택 시군, 학교급, 학생수 최소·최대를 교차 적용
  - 한 조건을 누를 때 다른 조건을 임의로 지우던 동작 제거
  - 현재 적용 조건 요약을 화면에 표시
- [x] 작동하지 않던 `교육혁신선도지역 해당/미해당` 버튼 제거
  - 공식 대상 명부·기준연도 미확보 상태를 명시
  - 명부 확보 전에는 임의로 작동하는 것처럼 보이지 않도록 처리
- [x] 전체 사용 안내 서랍 추가
  - 메뉴별 할 수 있는 작업, 지도 전환·복귀, 필터, AI, 파일 저장·인쇄 방법 안내
- [x] 전체 메뉴 공통 `현재 화면 Excel` 및 `인쇄/PDF` 도구 추가
  - 종합 대시보드, 학령인구 현황, 시뮬레이터, 주간 뉴스, 특수·다문화, 주요업무계획, 폐교 메뉴 지원
  - 뉴스는 최근 7일·30일 및 주제 필터 결과를 그대로 출력
  - 자료 미확보 메뉴는 값을 만들지 않고 자료 상태와 필요자료를 출력
- [x] 시뮬레이터 Excel을 5개 시트로 개편
  - `요약`, `기준자료`, `예측자료`, `변화시각화`, `필터와산출기준`
  - 시군 순서는 기능개선안의 22개 표준 순서로 고정
  - 변화율과 막대형 문자 시각화, 출처·예측 한계 포함
- [x] 시뮬레이터 화면 배치 보완
  - 예측연도 아래에 산출기준 빠른 선택 버튼 배치
  - AI 정책 검토 브리핑을 산출 설명 뒤 맨 아래로 이동
- [x] Gemini 상태 확인 기능 추가
  - `/api/data-status`에서 Cloudflare Secret 인식 여부 확인
  - 실제 분석 성공·무료 사용량 제한·키 미설정을 서로 다른 메시지로 안내
- [x] 자동 검사 145개 전부 통과
  - 실제 위치 지도 학생 합계, 인구감소지역 15곳, 다중필터, 5개 시트 Excel, 전체 메뉴 출력, 사용 안내를 회귀 검사에 추가

## 2026-08-27 지도·배포 진단·백엔드 기반

- [x] 지도 선택 버튼 순서를 `실제 위치 지도 → 간편 지도`로 변경
- [x] 실제 위치 지도에서는 시군 선택 여부와 관계없이 `← 경북 전체로`를 항상 표시
- [x] 실제 위치 지도 아래에 **현재 지도 화면 안의 학교 목록** 추가
  - 지도 이동·확대가 끝날 때마다 목록을 다시 계산
  - 경북 전체처럼 학교가 많을 때는 학생 수 순 120곳만 표시하고 전체 건수를 함께 안내
  - 로컬 실제 SGIS 화면에서 확대 전 1,425곳 → 두 단계 확대 후 49곳으로 목록 갱신 확인
- [x] 백엔드 기반을 코드로 구현
  - D1~D10 공통 데이터 목록, Cloudflare D1 초기 스키마, D1/R2 바인딩 견본
  - `/api/data-status` 상태 확인 API와 자동검사 추가
  - 실제 D1·R2 생성은 계정 소유권·운영 담당자 결정 뒤 진행하도록 분리
- [x] Gemini 미작동 원인 재확인
  - 키를 등록한 뒤 새 배포가 만들어지기 전에는 라이브 `/api/ai-analysis`가 HTTP 503을 반환
  - 2026-08-27 새 배포 뒤 `/api/data-status`에서 Gemini `configured: true` 확인
  - 실제 Google 요청은 HTTP 429 `RESOURCE_EXHAUSTED`를 반환해, 현재 남은 원인은 키가 아니라 무료 사용량 한도
  - 공식 가격표상 Gemini 3.7 Flash Standard는 무료 입력·출력을 지원하며, 한도는 Google AI Studio에서 프로젝트별 확인 필요
- [x] 로컬 Pages 환경 점검
  - SGIS Runtime 키는 인식되고 실제 지도·마커·학교 목록·전체 복귀 정상
  - 로컬 `.dev.vars`에는 새 Gemini 키가 없어 로컬 AI는 503이며, 운영 키 값은 전달받지 않고 Cloudflare Runtime에서 관리

## 2026-08-26 기능개선안 재점검·추가 반영

- [x] `경북 학령인구 시각화 대시보드 기능개선(안).pdf`의 전체 요구사항을 다시 대조
  - 반영·부분 반영·미반영·후속 결정 변경을 [기능개선안 PDF 반영 점검](./기능개선안_PDF_반영점검.md)에 표로 정리
  - 주요 미완료: 읍면동 집계, 지적도·관심시설, 설립유형·거주지 학교 필터, 주요업무계획 공식 이미지·조직도, 교육행정데이터 연계
- [x] 지도 명칭을 `간편 지도`와 `실제 위치 지도`로 바꿔 기술용어 노출을 줄임
- [x] 실제 위치 지도에서도 시군 선택 뒤 `경북 전체로` 버튼을 유지하고 전체 마커·전체 범위로 복귀
- [x] 시뮬레이터 비교표를 현재 조건 그대로 `Excel(.xls)` 파일로 저장
- [x] 화면 우측 슬라이드형 `용어 도움말` 추가
- [x] AI 기능을 표 아래의 강조 카드로 이동하고 용도를 명확히 설명
  - 학생수 변화 요약, 학교 운영 검토 신호, 정책 검토 질문, 추가 확인자료, 분석 한계 작성
  - 정책 자동 결정이나 확정 예측 기능이 아님을 명시
- [x] 지역 필터를 적용해도 `전체` 행과 AI 입력이 경북 전체값을 쓰던 오류 수정
  - 예: 영주시 중학교 선택 시 기준 2,306명, 2030 모의 1,870명만 분석
- [x] Gemini 3.7 Flash 공식 Interactions API 경로와 `thinking_level: low`로 정리
  - 결제를 연결하지 않은 무료 등급 프로젝트에서 사용하며, 무료 한도 초과 시 기본 정책 검토 요약으로 전환
- [x] 데이터 유지·인수인계를 위한 [데이터 운영·백엔드 준비안](./데이터_운영_백엔드_준비.md) 작성
  - 우선순위: 데이터 목록표 → 원본 스냅샷 → 표준 스키마 → 재현 가능한 수집·검사 → R2/D1 기반 작은 운영 백엔드

## 이번 결정 반영

- [x] 유치원 수를 `614`로 고정하지 않고 내장된 유치원 자료의 실제 개수를 세어 표시
  - 경북 전체에서는 614곳, 시군 상세에서는 해당 시군 값으로 전환(예: 영주시 23곳)
  - 초·중·고·유치원·특수학교 다섯 학교급을 모두 같은 방식으로 갱신
- [x] 공개 화면에 교원 수 KPI 추가
  - 초·중·고·특수학교는 학교알리미 공시값 사용
  - 유치원 교원 수는 현재 자료에 없으므로 `자료 미확보`로 표시
  - 전체 조회는 `유치원 제외`라고 명시
- [x] 다문화 및 특수교육 메뉴 유지
- [x] 출처 없는 소멸위험지수의 버튼·지도 계산·범례·시군 배지 삭제
- [x] 학교 순번으로 동·읍·면을 임의 배정하던 가짜 소재지 필터 삭제 및 비활성화
- [x] 임의 성별 분할 차트를 학교알리미 실제 학년별 학생수 차트로 교체
- [x] Gemini 3.7 Flash 분석 화면과 같은 출처 서버 중계 추가
- [x] 모바일 720px 이하 레이아웃 보완
- [x] 배포 저장소에 Cloudflare Pages Function이 함께 복사되도록 자동 배포 수정
- [x] API 키 파일 패턴을 `.gitignore`에 추가하고 키가 정적 HTML로 나가지 않도록 유지
- [x] KOSIS 기관 ID·통계표 ID를 직접 확인해 설정 파일과 자동 수집 도구에 반영
  - 기관 ID: `101`
  - 출생아·합계출산율: `DT_1B81A23`
  - 1세별 주민등록인구: `DT_1B04006`
  - 경북 시군 장래인구추계: `INH_PB0001_2022_37`
  - 5세별 순이동: `DT_1B26002`
- [x] KOSIS 2016~2025 주민등록인구와 2026~2036 장래인구추계를 대시보드 학령인구 시계열에 반영
- [x] SGIS 온라인 지도와 기존 SVG 오프라인 대체 지도를 선택할 수 있게 구현
  - SGIS 서비스 ID는 Cloudflare Pages Function에서만 사용
  - SGIS 응답·CSS는 같은 출처로 중계하고 정적 HTML에는 키를 넣지 않음
  - 온라인 로드 실패 시 오프라인 경계 지도를 유지
  - 로컬 Pages 환경에서 실제 지도 타일, 학교 좌표 1,538곳, 군집 클릭·확대와 개별 학교급 마커까지 확인
  - 군집 마커는 외부 이미지에 의존하지 않는 36px 숫자 마커로 보강
- [x] 유치원알리미 `기본현황(신규)` API의 공식 좌표로 갱신
  - 공식 좌표 613곳, 누락 1곳만 기존 주소 변환 좌표 유지
- [x] 굽기 스크립트의 무조건 라이브 배포를 중지하고, 명시적 `DEPLOY_LIVE=1`일 때만 수동 배포하도록 변경

## 배포 전에 해야 할 일

1. 대화에 노출된 모든 API 키를 각 발급기관에서 재발급 또는 폐기 후 재발급한다.
2. [Cloudflare Pages 프로젝트](https://dash.cloudflare.com/)의 **Settings → Variables and Secrets**에서 `GEMINI_API_KEY`와 `SGIS_CONSUMER_KEY`를 우선 암호화 Secret으로 등록한다. 나머지 키도 같은 이름으로 등록해 다음 데이터 갱신에 대비한다. 등록·교체 뒤에는 반드시 새 배포를 만든다.
3. Google Cloud의 해당 키를 **Generative Language API(Gemini API)로만 제한**하고 결제 계정을 연결하지 않는다. 무료 한도 초과 시 앱은 오프라인 요약으로 전환한다.
4. 무료 등급에서 전송 데이터가 Google 제품 개선에 사용될 수 있으므로 AI에는 집계값만 보내고 개인자료·학교 내부 비공개자료는 보내지 않는다.
5. 배포 후 `/dashboard/#sim`에서 `이 결과로 정책 검토안 만들기`를 눌러 응답과 한도 초과 메시지를 확인한다.

로컬 개발 키는 저장소 루트의 `.dev.vars`에 이미 정리했다. 이 파일은 커밋되지 않으며, 공유용 이름만 `.dev.vars.example`에 있다.

6. Cloudflare Pages를 GitHub `Kimmahone/team-leap`의 `main` 브랜치와 연결한다. 연결 전에는 GitHub Actions가 배포 저장소를 갱신해도 라이브 사이트가 자동으로 바뀌지 않는다.

## 추가 자료·결정 대기

### 온라인 지도

- [x] SGIS 서비스 ID와 보안 Key 한 쌍을 전달받았다. 화면에 노출된 값은 운영 적용 전에 재발급하고 Cloudflare Secret으로 등록해야 한다.
- [x] SGIS 온라인 지도 구현과 오프라인 대체 지도 전환을 완료했다.
- [SGIS 개발지원센터](https://sgis.kostat.go.kr/developer/html/openApi/api/intro.html)의 **인증키 발급 현황**에서 서비스 ID(`consumer_key`), 보안 Key(`consumer_secret`), 허용 도메인을 확인한다.
- [ ] 운영용 SGIS 키를 재발급한 뒤 허용 도메인에 실제 Pages 주소를 등록하고 라이브에서 타일·마커를 확인한다.

### 유치원

- [x] 유치원알리미 Open API 인증키와 `기본현황`, `직위·자격별 교직원현황` 제공 주소를 확인했다. 화면에 노출된 값은 운영 적용 전에 재발급한다.
- [x] `기본현황(신규)` 공식 좌표로 614곳을 다시 수집했다.
- [ ] `직위·자격별 교직원현황` API는 현재 제공기관의 데이터 점검으로 `DENIED`를 반환한다. 제공 재개 뒤 다시 굽기 전까지 교원 수를 추정하지 않고 `자료 미확보`로 둔다.
- [유치원알리미 Open API 이용안내](https://e-childschoolinfo.moe.go.kr/openApi/openApiIntro.do)에서 개발 또는 운영계정 인증키를 신청하고, [API 제공목록](https://e-childschoolinfo.moe.go.kr/openApi/openApiList.do)에서 교직원 현황 제공 항목·공시 차수를 확인한다.
- 오래된 공시 110곳의 운영 여부는 해당 교육지원청 또는 도교육청 내부 명부로 교차 확인한다.

### KOSIS·학교알리미

- [학교알리미 Open API](https://www.schoolinfo.go.kr/ng/go/pnnggo_a01_m0.do)의 2026 학교·학생·학급·교원 공시는 현재 내장되어 있다. 새 키는 다음 공시 갱신 때 사용한다.
- [x] [KOSIS 공유서비스](https://kosis.kr/openapi/index/index.jsp)의 기관 ID·통계표 ID·연령코드를 확정했고 `open api/kosis-tables.json`에 보관했다. 사용자가 KOSIS 화면에서 더 찾을 작업은 없다.
- [x] `open api/bake-kosis.mjs`로 공식 시계열을 내려받고 대시보드에 굽는 경로를 만들었다.
- 코호트 예측을 위해 최소 2016~2026년 출생아수와 학교알리미 학년별 학생수 과거 공시가 필요하다.

### 경북교육청 내부자료

- 상세한 우선순위·필드·예상 부서·공개 범위는 [경북교육청 내부자료 요청 목록](./경북교육청_내부자료_요청목록.md)에 정리했다.
- 현재 추가 자료 폴더는 정책 근거와 업무 절차를 보강했지만, 학교·연도별 원자료를 대신하지는 않는다.
- 공식 홈페이지 연계 방식은 추후 결정. 그 전까지 데이터 스키마와 API 경계를 분리해 유지한다.

## 아직 공개 판단에 쓰면 안 되는 부분

- 2027년 이후 학생 수는 코호트 예측이 아니라 임시 감소율을 적용한 모의값이다.
- 학령인구 시계열은 KOSIS 공식값으로 교체했지만, 2027년 이후 **학생 수** 시뮬레이션은 여전히 임시 감소율 모의값이다.
- 인구감소지역은 행정안전부 2021년 지정 문서의 경북 15개 지역으로 교체했다. 향후 재지정 고시가 나오면 갱신해야 한다.
- 다문화 학생 수는 아직 자료가 없어 비워 두었다.

## 검사 결과

- 대시보드 자동 검사: 145개 통과
- Gemini 중계 검사: 10개 통과
- 백엔드 기반 검사: 2개 통과
- KOSIS 수집 검사: 5개 통과
- SGIS 중계 검사: 9개 통과
- 전체 사이트 굽기: 2,428개 검사 전부 통과, 인증키 유출 0건, 정적 외부 CDN 0건
- SGIS 실제 인증 응답: HTTP 200, 약 320KB 지도 스크립트 수신, 서비스 ID가 응답에 포함되지 않음을 확인
- 로컬 Pages 화면 확인: 온라인 SGIS 지도 1,538곳·36개 초기 군집, 유치원 614곳, KOSIS 2026 합계 252천 명 정상 표시
- 2026-08-26 라이브 `team-leap.pages.dev` 자동 배포 완료. 영주시 상세의 학교급별 숫자와 유치원 23곳, 온라인 SGIS 지도의 영주시 유치원 마커 23곳을 실제 브라우저에서 확인했다.
- 커밋 `4bc41da` 자동 배포 성공. 라이브에서 `간편 지도`·`실제 위치 지도`, 영주시 선택 뒤 실제 위치 지도로 전환해도 남는 `경북 전체로` 버튼, 전체 복귀 뒤 유치원 614곳·전체 좌표 1,538곳을 확인했다.
- 2026-08-27 커밋 `2a28466` 자동 배포 성공. `/api/data-status`에서 Gemini 키 인식, SGIS 실제 스크립트 HTTP 200(약 320KB), 데이터 목록 10종을 확인했다. Gemini는 Google까지 요청이 도달하지만 HTTP 429 무료 할당량 제한을 반환하므로 Google AI Studio의 Rate limits에서 프로젝트 한도를 확인하고 잠시 뒤 재시도해야 한다.
- 커밋 `2284ae9` 배포 뒤 라이브 최종 확인: Gemini·SGIS·학교알리미·KOSIS·유치원알리미의 필수 환경변수 인식 상태가 모두 `true`; 실제 위치 지도 1,538곳, 현재 화면 목록 1,425곳, 두 단계 확대 뒤 49곳, `← 경북 전체로` 버튼 정상. AI 실행 시 429 원인을 보여 주고 검증된 기본 요약으로 자동 전환한다.
- 모바일 브라우저의 최종 육안 확인은 배포 전 390px·768px 두 너비에서 한 차례 더 수행한다.
