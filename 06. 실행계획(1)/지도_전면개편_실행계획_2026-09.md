# 종합 대시보드 지도 전면 개편 — 실행계획

작성: 2026-09-09 · 대상: `06. 실행계획(1)/prototype/index.html` 의 **종합 대시보드** 화면
계기: 9/9 GBE-AI 연구원(2기) 회의 — 「네이버 부동산 앱처럼 확대·축소하면서 학령인구 감소 추이가 보이도록」

---

## 0. 결론 먼저 — 세 문장

1. **지도 엔진은 바꾸지 않습니다.** 다른 팀 지도와의 차이는 엔진이 아니라 상호작용 설계입니다.
2. **학교별 과거 자료는 이미 받고 있습니다.** EDSS 에서 받아서 **굽는 과정에서 버리고 있었습니다.**
   새로 신청할 API 가 없습니다. 굽는 스크립트에서 내보내기만 하면 됩니다.
3. **왼쪽 요약 칸을 연도 슬라이더에 반응하는 자리로 바꿉니다.** 회의에서 말씀하신 구조가 맞습니다.

---

## 1. 다른 팀 지도와의 차이 — 엔진이 아니라 상호작용

### 1-1. GIS 와 SGIS 의 관계

**GIS 는 분야 이름이고, SGIS 는 그 분야의 서비스 하나입니다.** 다른 팀이 「GIS 를 쓴다」고
말한 것은 엔진을 특정한 말이 아닙니다. 네이버 부동산은 네이버 지도를, 우리는 통계청 SGIS 를
쓰고 있을 뿐 둘 다 GIS 입니다.

그리고 **우리가 쓰는 SGIS 는 Leaflet 계열 API** 입니다. 코드에서 이미 이렇게 부르고 있습니다.

| 지금 쓰고 있는 것 | 하는 일 |
|---|---|
| `sop.markerClusterGroup` | 가까운 학교를 하나로 묶기 |
| `sop.divIcon` | 말풍선 안에 값 적기 |
| `map.on('zoomend')` | 확대·축소에 반응하기 |
| `map.getBounds()` | 지금 보이는 범위만 세기 |
| `map.setView()` | 특정 학교로 날아가기 |

**네이버 부동산이 하는 동작 대부분이 이 다섯 개 위에서 만들어집니다.**
값이 적힌 말풍선도, 숫자가 든 묶음 원도 우리 지도에서 이미 돌고 있습니다.

### 1-2. 그래서 실제로 없는 것은 둘뿐입니다

| 없는 것 | 네이버 부동산에서 무엇에 해당하나 |
|---|---|
| **줌 단계별 집계 전환** | 시·도 → 구 → 동 → 단지로 바뀌는 것 |
| **연도 축** | 「5년 평당가 추이」 토글 |

나머지 차이(배경지도 미감, 정보 밀도)는 취향과 다듬기의 영역입니다.

### 1-3. 엔진을 바꾸지 않는 이유

| | 바꿀 때 잃는 것 | 얻는 것 |
|---|---|---|
| CSP | `/dashboard/*` 의 `style-src`·`img-src`·`connect-src` 를 새 도메인으로 다시 씀 | — |
| 인증키 | `functions/api/sgis-map.js` 프록시를 새로 만듦 | — |
| 오프라인 대비 | SGIS 실패 시 SVG 지도로 내려앉는 이중화를 다시 검증 | — |
| 행정경계 | SGIS 는 **통계 경계에 강합니다.** 읍면동 단계를 만들 때 이게 필요합니다 | — |
| 배경지도 | | 미감이 나아짐 |

**얻는 것이 미감 하나뿐입니다.** 회의에서 미감이 실제 지적사항으로 올라오면
그때 VWorld(국토부, 무료, 행정경계 제공)를 후보로 다시 봅니다. 지금은 아닙니다.

---

## 2. ★ 학교별 과거 자료 — 이미 받고 있고, 버리고 있습니다

**이번 점검에서 가장 중요한 발견입니다.**

### 2-1. 무슨 일인가

EDSS 의 `유초중등학생현황` · `유초중등학급현황` 두 API 는
**학교(`opnId`) × 학년 × 연도** 단위로 옵니다. 2016~2025 열 해치 **97,134행**을
이미 받고 있습니다.

그런데 `open api/bake-edss.mjs` 의 `aggregate()` 가 이렇게 접습니다.

```
학교 × 학년 × 연도   →   시군 × 학교급 × 연도
```

접고 난 뒤 **학교 단위 원자료는 어디에도 저장되지 않습니다.**
`edss-series.json` 에도 시군별·학년별 집계만 들어 있습니다.

코드 주석은 이렇게 적고 있습니다.

> 학교·학년 단위를 시군×학교급×연도로 접습니다. 대시보드가 필요한 것은 여기까지입니다 —
> 학교별 여러 해치를 화면에 넣으면 400KB 가 됩니다.

**그때는 맞는 판단이었습니다.** 화면에 넣을 데가 없었으니까요.
이제 연도 슬라이더가 그 자리입니다.

### 2-2. 그래서 무엇을 하면 되나

| | |
|---|---|
| 새로 신청할 API | **없습니다** |
| 고칠 곳 | `bake-edss.mjs` 에 내보내기 한 벌 추가 |
| 나오는 것 | `open api/data/school-series.json` |
| 담을 것 | 학교코드 → 연도별 학생수·학급수 (2016~2025) |

### 2-3. 크기와 싣는 법

| 방식 | 크기 어림 | 판단 |
|---|---|---|
| 학교 × 연도 **합계**만 (학생수·학급수) | 약 60~90KB | **인라인 가능** |
| 학교 × 연도 × **학년별**까지 | 약 400KB | 별도 파일 + 지연 로딩 |

**별도 파일도 문제없습니다.** `/dashboard/*` 의 CSP 는 `connect-src 'self'` 라
같은 출처 파일은 부를 수 있습니다. 이미 `/api/ai-analysis` 를 그렇게 부르고 있습니다.

> 권고: **합계는 인라인, 학년별은 별도 파일**로 나눠 슬라이더를 처음 만질 때 불러옵니다.
> 첫 화면이 무거워지지 않고, 안 쓰는 사람은 받지 않습니다.

### 2-4. 그래도 못 채우는 자리

| | 왜 |
|---|---|
| 2026년 학교별 | EDSS 조사연도가 아직 2025 까지입니다. 2026은 학교알리미 공시로 한 해만 |
| 유치원·특수학교 과거 | EDSS 유초중등 두 API 가 초·중·고 중심입니다. 확인 필요 |
| 폐교의 과거 학생수 | 폐교 시점 이전 자료를 EDSS 가 주는지 확인 필요 |

**슬라이더를 만들 때 이 세 자리를 화면이 스스로 말하게 해야 합니다.**
없는 해에 선을 이으면 그것이 곧 지어낸 자료가 됩니다.

---

## 3. 새 구조 — 왼쪽 칸을 연도 슬라이더에 붙입니다

### 3-1. 회의에서 말씀하신 구조가 맞습니다

네이버 부동산의 왼쪽 칸은 **지도에서 고른 것의 상세**입니다.
지금 우리 왼쪽 칸은 **지도와 무관한 고정 요약**입니다. 이것을 바꿉니다.

| 지금 왼쪽 칸 | 바뀐 뒤 |
|---|---|
| 경북 학령인구 현황 (고정값) | **선택 지역 × 선택 연도**의 학생수·학급수 |
| 경북 학생수 현황 (고정값) | **연도별 추이 꺾은선** — 슬라이더를 끌면 함께 움직임 |
| 학령인구 지표 (고정값) | 선택 지역의 학교당·학급당 평균 |
| 주간 AI 뉴스 요약 | **아래로 내리거나 뉴스 화면으로 옮김** (아래 설명) |

### 3-2. 뉴스 요약은 슬라이더에 붙이지 않습니다

**뉴스에는 2016~2036 축이 없습니다.** 연도 슬라이더를 2018년에 놓았을 때
「2018년의 주간 뉴스」는 존재하지 않습니다. 슬라이더 옆에 두면 사용자는
당연히 연동될 것으로 기대하고, 연동되지 않으면 고장으로 읽습니다.

> 뉴스 요약 카드는 **왼쪽 칸 맨 아래로 내리고 슬라이더 영역 밖에 둡니다.**
> 시각적으로도 구분선을 넣어 「이 위는 연도에 반응하고, 아래는 아니다」를 보이게 합니다.

### 3-3. 줌 단계와 화면의 대응

| 줌 | 지도에 보이는 것 | 왼쪽 칸 | 연도 슬라이더 |
|---|---|---|---|
| 경북 전체 | **시군 22개 원** — 안에 학생수 | 경북 합계 + 추이 | **작동** |
| 시군 | **읍면동 집계 원** | 그 시군 + 추이 | **작동** |
| 읍면동 | **학교 말풍선** — 학생수와 증감률 | 그 지역 학교 목록 | 2026 고정 |
| 학교 클릭 | 말풍선 상세 | 그 학교 상세 | **2-2 완료 후 작동** |

### 3-4. 네이버 부동산에서 그대로 가져올 것

| 저쪽 | 우리 |
|---|---|
| 하단 「매물 212 · 단지 31」 | 하단 「학교 N곳 · 학생 N명」 |
| 「용흥동만 보기」 | 「이 시군만 보기」 |
| 오른쪽 세로 레일(교육·편의·개발) | 레이어 토글(폐교·유치원·특수·통학반경) |
| 상단 필터 칩 | 학교급·규모·증감률 |

---

## 4. 단계와 기한

| 단계 | 무엇 | 자료 준비 | 크기 |
|---|---|---|---|
| **1** | 줌 문턱 세 개로 집계 단위 전환 (시군 원 ↔ 학교 말풍선) | **지금 있는 것으로 됨** | 2일 |
| **2** | 왼쪽 칸을 지도 선택에 동기화 · 뉴스 카드 분리 | 지금 있는 것으로 됨 | 1일 |
| **3** | **연도 슬라이더 — 시군 단위** | `EDSS_SGG` 로 **지금 됨** | 2일 |
| **4** | `bake-edss.mjs` 에 학교별 시계열 내보내기 추가 | **코드 완료** · Actions 1회 실행 남음 | 1일 |
| **5** | **연도 슬라이더 — 학교 단위** | **코드 완료** · 4단계 자료 오면 켜짐 | 2일 |
| **6** | 읍면동 단계 (좌표를 행정동으로) | `kakao-geocode.mjs` 재사용 | 1.5일 |
| **7** | 레이어 토글 · 하단 집계 표시 · 다듬기 | — | 2일 |

**1·2·3 단계는 지금 심어진 자료만으로 끝납니다.** 시군 단위 연도 슬라이더가
먼저 돌아가는 것을 보고 나서 학교 단위로 내려가는 순서입니다.

### 4-1. 4단계는 왜 따로인가

EDSS 인증키가 **GitHub Actions Secrets 에만** 있고 로컬에는 없습니다.
그래서 학교별 시계열은 로컬에서 굽지 못하고 Actions 에서 한 번 돌려야 합니다
(`workflow_dispatch` 의 `edss`).

### 4-2. 〔2026-09-09〕 코드는 끝났습니다 — 남은 것은 Actions 1회

키 없이 검증한 방법과 결과입니다.

| 무엇 | 어떻게 | 결과 |
|---|---|---|
| 굽는 쪽 순수 함수 | `schoolSeries` · `toSchoolBlock` 을 지어낸 records 로 검사 | 검사 통과 |
| 굽기→화면 한 바퀴 | SCHOOL_RAW 917곳에 **가짜 열 해치**를 심어 사본을 만듦 | **917곳 전부 붙음 · 못 붙임 0** |
| 크기 | 심기 전후 비교 | 672KB → **737KB (+65KB)** — 인라인으로 충분 |
| 화면의 «심은 뒤» 갈래 | 그 사본으로 대시보드 검사를 다시 돌림 | 285개 전부 통과 |

**Actions 를 돌리는 순간 켜집니다.** 그 전까지 화면은 예전 그대로 돌고,
지난 해를 물으면 «없다»고 답합니다. 검사가 양쪽 갈래를 모두 지킵니다.

굽는 쪽에 **관문**을 하나 두었습니다 — 학교 이름이 **90% 미만**으로 붙으면
심지 않고 멈춥니다. 절반만 붙어도 화면은 조용히 그려지고, 사람은
「어떤 학교는 슬라이더가 안 먹네」로만 느낍니다. 원인을 찾을 수 없는 종류의 고장입니다.

---

## 5. 지금 당장 되는 것과 안 되는 것 — 한눈에

| | 상태 |
|---|---|
| 시군 × 학교급 × 연도 학생수·학급수 (2016~2025) | **인라인에 있음** — `EDSS_SGG` |
| 도 전체 학년별 × 연도 | **인라인에 있음** — `EDSS_GRADE` |
| 학교 × 2026 학생수·학급수·학년별 | **인라인에 있음** — `SCHOOL_RAW` |
| **학교 × 연도 (2016~2025)** | **EDSS 에 있으나 굽는 중에 버려짐** — 4단계 |
| 학교의 읍면동 | **없음** — 주소로는 불가(시 지역 주소에 읍면동이 없음), 좌표 변환 필요 |
| 읍면동 경계 도형 | **없음** — 지금 경계 파일은 시군 단위 |

---

## 6. 저장소를 Public 으로 바꿀 때

회의에서 나온 질문에 대한 답입니다.

### 6-1. 확인한 것

| | 결과 |
|---|---|
| `team-leap-source` 현재 상태 | **Private** |
| 커밋 이력에 실제 인증키 | **없음** (182개 커밋 전수 검색) |
| 걸린 것 | `tests/edss.test.mjs` 의 **가짜 상수** 뿐 |
| `open api/인증키.txt` | 추적되지 않음 (`.gitignore` 가 막고 있음) |
| `.dev.vars` | 추적되지 않음 |

### 6-2. Public 으로 바꾸면

**추가 설정은 필요 없습니다.** 그리고 덤이 셋 있습니다.

| | |
|---|---|
| Collaborator 초대 | **불필요해집니다** — 링크만 주면 누구나 읽습니다 |
| Actions 사용량 | 공개 저장소는 **무제한 무료** |
| 브랜치 보호 규칙 | 공개 저장소는 **무료** — 9/9 회의의 A안/B안 월 $4 논의가 사라집니다 |
| Secret scanning | 공개 저장소는 **무료로 켜집니다** |

**Actions Secrets 는 공개해도 노출되지 않습니다.** 암호화되어 있고 로그에도 가려집니다.

### 6-3. 다만 바꾸기 «전에» 확인할 것

- [ ] `배포/site/` 쪽 저장소(`team-leap`)도 같은 검사를 한 번 돌릴 것
- [ ] 문서에 적힌 내부 정보(부서명·담당자·미공개 계획)를 공개해도 되는지 판단
- [ ] 바꾼 직후 **Secret scanning 경고를 한 번 확인**

> 코드에는 문제가 없습니다. 판단이 필요한 것은 **문서 쪽**입니다.
> `98. 노션/` 과 `2. 깃허브 미탑재 (참고자료)/` 는 애초에 저장소 밖이라 해당 없습니다.

---

## 7. 회의에서 나온 나머지 항목 — 가능 여부

| 항목 | 가능한가 | 언제 |
|---|---|---|
| 뉴스 키워드 빈도·워드클라우드·다빈도 | **가능** — 굽는 단계에서 순수 JS | 지도 다음 |
| 기사 벡터화·유사도 묶기 | **가능** — TF-IDF + 코사인, 의존성 0 | 〃 |
| 시군별 뉴스 크롤링 | **가능** — 검색어에 22개 시군 추가 | 〃 |
| 이슈페이퍼 생성·아카이브 | **가능** — Gemini 초안 + 사람 검토 | 〃 |
| 시군별 대응 진단지수 | **가능** — 지금 자료만으로 하위지표 5개 | 10월 |
| 페르소나 시뮬레이션 | 별도 프로젝트 | 이 문서 밖 |

**전부 시간이 걸릴 뿐 막힌 것은 없습니다.** 유일하게 자료가 없어 막히는 것은
「학교별 2026년 이후 시계열」이고, 그것은 EDSS 조사연도가 열려야 풀립니다.

---

## 8. 다음 손댈 곳

1. 1단계 착수 — 줌 문턱과 시군 집계 원
2. Actions 에서 `edss` 를 한 번 돌려 학교별 시계열 확보 (4단계 준비)
3. 저장소 Public 전환 판단 — 문서 쪽만 검토하면 됩니다

---

*이 문서의 모든 확인값은 2026-09-09 로컬 저장소와 `node test.js` 실행 결과 기준입니다.*


---

# 9. 브이월드 위성지도 — **미룸** 〔2026-09-10 기록〕

키까지 발급받아 붙여 보았고, **길이 막혀 있음을 확인하고 미룹니다.**
다음 사람이 같은 길을 다시 파지 않도록 무엇을 확인했는지 남깁니다.

## 9-1. 무엇이 막혔나

| | 위성영상 | EPSG:5179 (우리 지도 좌표계) |
|---|---|---|
| 브이월드 **WMS** | **없음** — 레이어 355개가 전부 주제도 | 지원함 |
| 브이월드 **WMTS** | 있음 (타일 정상 수신 확인) | **없음** — EPSG:3857 하나뿐 |

SGIS 지도는 UTM-K 위에서 돌고 `sop.LatLng` 이 위경도를 받는 즉시 그 좌표계로
투영합니다(`r.LatLng.prototype = r.UTMK.prototype`). 그래서 3857 타일은 그대로
얹히지 않습니다. **위성이 있는 쪽에는 우리 좌표계가 없고, 우리 좌표계가 있는
쪽에는 위성이 없습니다.**

> 처음에 「WMS 로 얹으면 된다」고 판단한 것은 **틀렸습니다.** 지원 좌표계만 보고
> 레이어 목록을 확인하지 않았습니다. GetCapabilities 를 받아 세어 보고 알았습니다.

## 9-2. 곁들여 확인한 것 (다음에 그대로 씁니다)

- 브이월드 WMS 는 **VERSION=1.3.0 만** 받습니다. 1.1.1 은 거절합니다.
- 1.3.0 에서는 인자 이름이 `SRS` 가 아니라 **`CRS`** 입니다.
- `domain` 은 **호스트명만** 보냅니다. `https://` 를 붙이면 `INCORRECT_KEY` 입니다.
- 발급된 키는 **`team-leap-source.pages.dev` 에만** 등록되어 있습니다.
  운영(`team-leap.pages.dev`)에서 쓰려면 그 도메인을 더 등록해야 합니다.
- **Cloudflare 함수로는 중계할 수 없습니다.** 브이월드 응답이 비표준이라
  (`HTTP/1.1 200 200` · `Connection: Upgrade`) Workers 의 `fetch` 가 거부합니다.
  같은 요청이 사람 컴퓨터에서는 200 입니다. 브라우저가 직접 부르면 됩니다.

## 9-3. 남겨 둔 것

- `functions/api/vworld-key.js` — 브이월드 키 하나만 내려 줍니다 (도메인 제한 방식이라 정상 사용법)
- `functions/api/vworld-probe.js` — 브이월드에 물어보고 답만 돌려줍니다
- 화면의 위성 단추는 `VWORLD_SAT_READY = false` 로 **내려 두었습니다**

## 9-4. 다시 할 때의 두 갈래

| 안 | 무엇 | 크기 | 위험 |
|---|---|---|---|
| **A** | 웹 메르카토르 지도를 따로 세워 WMTS 위성을 얹음 | 중간 | 마커 파이프라인이 UTM-K 라 좌표가 어긋날 수 있음 |
| **B** | WMS 로 **주제도**를 얹음 (용도지역 등). 위성은 포기 | 작음 | 위성이 아님 |

A 는 `sop.CRS.Proj`(proj4 기반)가 있어 새 라이브러리 없이 될 «가능성»이 있습니다.
다만 `sop.LatLng` 이 UTM-K 로 투영해 버리는 것을 어떻게 다룰지가 관건입니다.

**언박싱 데이 뒤에 다시 봅니다.**

---

# 10. 지도를 새로 세웠습니다 — 「지도로 보기」 〔2026-09-10 기록〕

9장은 **SGIS 위에** 브이월드를 얹으려던 기록입니다. 그 길은 접었습니다.
아래는 **엔진을 바꿔** 새로 세운 기록입니다.

## 10-1. 왜 접었나

SGIS 의 `sop` 은 Leaflet 0.7 을 UTM-K 전용으로 고친 것입니다. 브이월드 타일은
웹 메르카토르라 좌표계가 어긋납니다. 어긋난 자리를 두 번 고쳤고, 두 번 다
**빈 화면**이었습니다. 오류조차 나지 않아 브라우저 밖에서는 무엇이 막혔는지
알 길이 없었습니다. 손으로 검증한 타일 번호(z8 → 219/100)는 브이월드와 정확히
같았는데도 그랬습니다.

MapLibre 는 웹 메르카토르가 기본이고, 브이월드 타일이 바로 그 좌표계입니다.
셈이 필요 없습니다. 갈아탄 첫날 지도가 나왔습니다.

## 10-2. 무엇으로 세웠나

| 자리 | 무엇 | 어디서 |
|---|---|---|
| 지도 엔진 | MapLibre GL JS 4.7.1 | 저장소 안 `prototype/vendor/` (외부 CDN 아님) |
| 배경지도 | 브이월드 WMTS — 일반·위성·위성+지명·야간 | `api.vworld.kr` |
| **고도** | AWS Terrain Tiles (Terrarium) | `s3.amazonaws.com` |
| 학교 위치·학생 수 | 대시보드가 이미 갖고 있는 `SCHOOLS` | 학교알리미·EDSS 공시 |
| 시군 경계 | `GB_POLY` + `gbRings()` | 간편 지도와 **같은 자료** |

고도는 브이월드에 **없습니다**. `hillshade`·`terrain`·`dem` 을 하나씩 불러
모두 500 을 받았습니다. AWS Terrain Tiles 는 키 없이 열려 있고 타일 번호가
같아 그대로 맞습니다.

## 10-3. 타일에서 걸린 것 두 가지

1. **레이어마다 확장자가 다릅니다.** Base·Hybrid·midnight 는 `png`,
   Satellite 는 `jpeg` 입니다. 틀리면 HTTP 200 으로 «오류 XML» 이 와서
   **조용히 빈 화면**이 됩니다.
2. **Hybrid 는 단독 배경이 아니라 덮개입니다.** 받아서 재어 보니 Base 는
   39.9KB, Hybrid 는 9.6KB — 길과 지명만 든 투명 그림입니다. 위성 **위에**
   얹어야 「위성+지명」이 됩니다.

## 10-4. 대시보드에 넣은 방법 — 왼쪽 메뉴 한 자리

종합 대시보드의 지도 칸은 **한눈에 보는 요약**으로 남겼습니다. 자세히 볼
자리로 「지도로 보기」 메뉴를 하나 만들고 화면을 통째로 씁니다.

- MapLibre(800KB)는 **이 화면을 열 때만** 불러옵니다. 뉴스만 보러 온 사람에게
  지우지 않습니다.
- 학교 자료는 `schools.json` 이 아니라 **메모리에 이미 있는 `SCHOOLS`** 를 씁니다.
  두 벌을 쓰면 한쪽만 갱신되어 두 화면이 다른 수를 말하게 됩니다.
- 학교를 누르면 왼쪽 칸이 펼쳐집니다. 카드는 종합 대시보드가 쓰는 것과
  **같은 함수**(`schoolCardHtml`)입니다. 보는 해만 화면마다 다르게 넘깁니다.

## 10-5. 옮겨 붙인 것

폐교 겹쳐 보기 · 연도 슬라이더(2016–2036) · 학교 검색 · 시군 경계 · 거리 재기.

거리 재기는 SGIS 가 주던 자라 새로 만들었습니다. 학교 두 곳을 누르면 그
사이 직선거리가 나옵니다. **직선거리입니다** — 산을 넘는 길은 이보다 멉니다.
재는 동안에는 말풍선을 떼어, 학교를 누르면 재는 점이 되게 했습니다.

## 10-6. 만들면서 걸려 넘어진 것 (다음에 같은 데서 넘어지지 않게)

- **`return` 뒤에 줄을 바꾸면** 자바스크립트가 거기서 문장을 끝냅니다.
  카드가 통째로 빈 값이 되었습니다. 검사가 잡았습니다.
- **CSS 변수를 MapLibre 의 `paint` 에 넣을 수 없습니다.** 게다가 이 앱의 토큰은
  `--brand:var(--leap-blue)` 처럼 **별칭**이라, `getPropertyValue` 로 읽으면
  `var(--leap-blue)` 라는 **글자**가 나옵니다. 시군 경계가 통째로 안 그려진
  까닭이 이것입니다. 빈 칸을 하나 만들어 브라우저에게 풀게 한 뒤 읽습니다.
  딱지마다 부르는 자리라 값은 쟁여 두고, 화면 밝기가 바뀌면 비웁니다.
- **본문 위쪽으로 여백을 물리면 안 됩니다.** 그 자리에 「사용 안내·인쇄」 줄이
  있습니다. 높이는 식으로 셈하지 않고 **실제로 재서** 넣습니다.
- **MapLibre 는 딱지에 `transform` 을 적어 넣습니다.** 같은 자리에 CSS 로
  `rotate` 를 주면 덮어써져 사라집니다 (폐교 마름모가 네모가 됩니다).
- **WebGL 이 없는 형편**(오래된 업무용 PC·원격 데스크톱)에서는 지도가 만들어지는
  자리에서 멎습니다. 감싸지 않으면 「불러오는 중」에서 영영 멈춘 것처럼 보입니다.

## 10-7. 되돌릴 자리

- 태그 `backup/sgis-map-2026-09-10` · 브랜치 `backup/sgis-map`
- `prototype/map-new.html` — 대시보드에 옮기기 전의 **시험판**입니다.
  비교할 자리로 한동안 함께 올리다가, 손볼 일이 없어지면 지웁니다.

## 10-8. 남은 것

- 운영 도메인(`team-leap.pages.dev`)에 브이월드 키를 **등록해야** 배경지도가 나옵니다.
  지금 발급된 키는 `team-leap-source.pages.dev` 에만 걸려 있습니다.
- 확대했을 때 이름표가 겹칩니다. 학생이 많은 학교를 위로 올려 두었지만,
  겹침 자체를 피하려면 딱지를 캔버스 안(심볼 레이어)으로 옮겨야 합니다.
  그러면 MapLibre 가 스스로 겹치는 것을 숨겨 줍니다. **다음 손댈 자리입니다.**

---

# 11. 써 보고 고친 것 일곱 〔2026-09-10 · 2차〕

미리보기를 써 보시고 주신 말씀을 그대로 옮겨 적고, 무엇을 했는지 답니다.

| # | 말씀 | 한 일 |
|---|---|---|
| 1 | 3D 에서 돌릴 수 있으면 좋겠다 | **Shift + 끌기**로 돌립니다. 좌우는 방위, 위아래는 기울기 |
| 2 | 학교 추이선이 스파크라인뿐이라 보기 힘들다 | 가로 눈금 3줄 + 그 값, 해마다 세로 눈금, 양 끝 점 |
| 3 | 경북 전체 학생 수가 뭉쳐 있다 | 초·중·고를 갈라서 보여 줍니다 (종합 대시보드와 같은 모양) |
| 4 | 「학교 이름까지 봅니다…」 안내를 없애 달라 | 지웠습니다. 상태줄은 «말할 일이 있을 때만» 뜹니다 |
| 5 | 거리 재기가 점 두 개뿐이다 | 여러 점을 찍습니다. 구간마다 거리 + 합, 되돌리기·지우기 |
| 6 | 위쪽 단추와 +·− 단추가 겹친다 | 그 줄이 끝나는 자리를 «재서» 지도를 그만큼 내렸습니다 |
| 7 | 왼쪽 칸을 접을 수 없다 | 칸마다 접는 손잡이. 접은 자리는 이 브라우저에 남습니다 |

## 11-1. 하면서 또 걸린 것

- **지도 안에 글자를 세우려면 글꼴 서버(`glyphs`) 주소가 있어야 합니다.**
  거리 재기의 번호를 `symbol` 레이어로 넣었더니 통째로 안 보였습니다. 오류도
  나지 않습니다. 번호는 **DOM 딱지**로 그려 브라우저 글꼴을 씁니다.
- **스타일이 서기 전에는 `getSource` 가 빕니다.** 지도가 뜨기 전에 거리 재기를
  켜면 선을 영영 못 봤습니다. 이제 «글 쓰는 일»과 «그리는 일»을 나눠, 글은
  바로 적고 그림은 스타일이 선 뒤에 한 번 더 그립니다.
- **찍은 자리가 학교 이름표에 가렸습니다.** 이름표는 학생 수만큼(최대 400)
  위로 올라갑니다. 찍은 자리는 600 으로 그보다 위에 둡니다.
- **접기는 지도보다 먼저 답니다.** 지도를 못 만드는 형편(WebGL 없음)에서도
  왼쪽 칸은 써야 합니다.

## 11-2. PR #21 은 닫았습니다

레이어마다 확장자가 다르다는 것은 이 브랜치에 이미 들어 있고, 그 PR 이
고치던 «옛 SGIS 지도의 브이월드 배경» 자체가 없어졌습니다.

---

# 12. 접기를 다시 만들고, 종합 대시보드를 치웠습니다 〔2026-09-10 · 3차〕

## 12-1. 카드마다 접기 → 왼쪽 칸 통째로 접기

카드를 하나씩 접게 만들었더니 세 가지가 어긋났습니다.

- **다 접어도 지도가 넓어지지 않았습니다.** 칸의 너비가 그대로였기 때문입니다.
  접는 뜻은 자리를 아끼는 것이 아니라 «지도를 넓게 보는 것»이었습니다.
- **화살표가 엉뚱한 자리에 섰습니다.** 제목 줄을 flex 로 만들면서
  `span:first-child` 에 «맨 앞 칸을 차지하라»고 했는데, 제목이 글자로만 있는
  카드(배경·보기·연도)에서는 그 규칙이 **화살표에 걸렸습니다.**
- 손잡이가 일곱 개라 어느 것을 눌러야 하는지도 알기 어려웠습니다.

손잡이를 **하나**로 바꿨습니다. 「요약 접기」를 누르면 왼쪽 칸이 통째로
사라지고 그 너비를 지도가 가져갑니다. 단추는 **지도 위에** 둡니다 — 칸 안에
두면 접은 뒤 함께 사라져 돌아올 길이 없어집니다.

★ 만들면서 걸린 것 — `grid-template-columns:0 minmax(0,1fr)` 로 두었더니 지도가
**손톱만 해졌습니다.** `display:none` 인 것은 격자에서 아예 빠지므로 지도가
그 0 짜리 첫 칸에 들어갑니다. 접었을 때는 «칸이 하나»라고 적어야 맞습니다.

## 12-2. 종합 대시보드를 치웠습니다 (지우지 않았습니다)

지도가 제 화면을 갖게 되면서 이 요약 화면이 하던 일이 겹쳤습니다.
메뉴에서 빼고 화면은 `parked-home` 보관 칸에 담았습니다.

**왜 주석이 아니라 `<template>` 인가.** 이 화면 안에는 `var(--line)` 같은 CSS
변수가 42곳, 겹친 주석이 19곳 있습니다. HTML 주석은 붙임표 두 개를 품을 수
없고 겹쳐 쓸 수도 없습니다. `<template>` 안의 것은 화면에 그려지지도,
`getElementById` 로 잡히지도 않습니다 — 주석과 같은 효과이면서 안전합니다.

> 이 설명을 처음 적을 때 본문에 붙임표 두 개를 그대로 썼다가 **거기서 주석이
> 끊겨 설명문이 화면 맨 위에 쏟아졌습니다.** 설명하던 함정에 그대로 빠진
> 것입니다. 이제 검사가 「주석 안에 붙임표 두 개가 없다」를 봅니다.

**되살리는 법 세 걸음** — ① `<template>` 여는 줄과 닫는 줄을 지웁니다
② 메뉴에서 「종합 대시보드」 단추의 주석을 풉니다 ③ `HOME_TAB_ON` 을 true 로.

## 12-3. 화면 하나를 치우다 앱 전체가 멎었습니다

가장 큰 고비였습니다. 화면을 치우자 **지도 화면조차 열리지 않았습니다.**

까닭은 최상위에서 바로 실행되던 줄들이었습니다.

```js
document.getElementById('home-level').addEventListener('change', …)
```

문서에 없는 것을 만지면 `null` 에 걸려 **스크립트가 통째로 멎습니다.**
그 뒤 줄이 하나도 실행되지 않아 `showView` 도, 지도도 살아나지 못했습니다.
오류는 브라우저 콘솔에만 남고 화면에는 「빈 화면」으로만 보입니다.

`onEl(id, ev, fn)` 도우미를 만들어 열한 곳을 바꿨습니다. 없으면 조용히
지나갑니다. **화면 하나를 치우는 일이 다른 화면을 끄는 일이 되어서는
안 됩니다.**

검사도 함께 바꿨습니다. 치워 둔 화면의 그리는 코드를 **손으로 불러서**
확인합니다 — 되살릴 때 고장 나 있으면 치워 둔 뜻이 없습니다.

검사 3,652개 전부 통과.
